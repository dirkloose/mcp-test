# Base URL for LM Studio/API Endpoint
LM_BASE_URL=http://localhost:1234

# API Key (If the endpoint requires a bearer token)
LM_API_KEY=

# Model id as reported by GET /v1/models on your local server
LM_MODEL=